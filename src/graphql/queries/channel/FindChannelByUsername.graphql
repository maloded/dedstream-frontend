query FindChannelByUsername($username: String!) {
	findChannelByUsername(username: $username) {
		id
		username
		displayName
		bio
		avatar
		updatedAt
		isVerified
		stream {
			thumbnailUrl
		}
	}
}
