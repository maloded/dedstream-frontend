query FindChannelByUsername($username: String!) {
	findChannelByUsername(username: $username) {
		id
		username
		displayName
		bio
		avatar
		updatedAt
		isVerified
		socialLinks {
			title
			url
		}
		stream {
			title
			thumbnailUrl
			isLive
			category {
				title
			}
		}
		sponsorshipPlans {
			id
			title
			description
			price
		}
		followings {
			id
		}
	}
}
