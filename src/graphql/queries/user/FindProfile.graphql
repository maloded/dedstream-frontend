query FindProfile {
	findProfile {
		username
		displayName
		email
		avatar
		bio
		isVerified
		isTotpEnabled
		updatedAt
		notificationSettings {
			siteNotifications
			telegramNotifications
		}
		stream {
			serverUrl
			streamKey
			isChatEnabled
			isChatFollowersOnly
			isChatPremiumFollowersOnly
		}
	}
}
