subscription ChatMessageAdded($streamId: String!) {
    chatMessageAdded(streamId: $streamId) {
        createdAt
        text
        user {
			id
			username
			updatedAt
			avatar
			isVerified
        }
    }
}